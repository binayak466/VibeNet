name: vibenet
description: "A new Flutter social app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  flutter_contacts: ^1.1.9+2
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
  
